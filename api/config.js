const config = {
  db: {
    host: "localhost",
    user: "root",
    password: "", // XAMPP default nema lozinku
    database: "skola_jezika",
    connectTimeout: 60000,
    namedPlaceholders: true,
  },
};

module.exports = config;
