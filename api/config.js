const config = {
  db: {
    host: "localhost", // www.veleri.hr 
    user: "root", // 1g
    password: "", // XAMPP default nema lozinku, 1g
    database: "skola_jezika", // 1g
    connectTimeout: 60000,
    namedPlaceholders: true,
  },
};

module.exports = config;
